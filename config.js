const { TFM } = require('./constants')
module.exports = {
    USE_PROXIES: false,
    USE_CACHE: true,
    TEXTFIELD_MODE: TFM.EMPTY, // can be EMPTY, RANDOM_SENTENCE or RANDOM_STRING
    FORM_URL: ''
}
